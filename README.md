git clone https://github.com/beanisnotgay-debug/system-config.git
cd system-config
sudo ./install.sh




The installer will:
1. Install all packages from packages.txt
2. Copy configs to ~/.config/
3. Copy shell configs (.bashrc, .zshrc, etc.)
Note: AUR packages need manual install with yay/paru since I filtered those out.
